# OpenClaw Configuration

This directory contains OpenClaw configuration snippets.

## Current Config Summary

```json
{
  "auth": {
    "profiles": {
      "github-copilot:github": {
        "provider": "github-copilot",
        "mode": "token"
      },
      "modal:llm": {
        "provider": "modal",
        "mode": "api_key"
      }
    }
  },
  "models": {
    "mode": "merge",
    "providers": {
      "modal": {
        "baseUrl": "https://api.us-west-2.modal.direct/v1",
        "api": "openai-completions",
        "models": [
          {
            "id": "zai-org/GLM-5-FP8",
            "name": "GLM-5",
            "reasoning": true,
            "contextWindow": 192000,
            "maxTokens": 8192
          }
        ]
      }
    }
  },
  "agents": {
    "defaults": {
      "model": {
        "primary": "modal/zai-org/GLM-5-FP8"
      }
    }
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "dmPolicy": "allowlist",
      "groupPolicy": "allowlist",
      "streamMode": "partial"
    }
  }
}
```

## File Locations

| File | Path |
|------|------|
| Main Config | `~/.openclaw/openclaw.json` |
| Workspace | `~/.openclaw/workspace` |
| Serper Script | `~/.openclaw/workspace/serper-search.sh` |

## Commands

```bash
# View current config
cat ~/.openclaw/openclaw.json

# Edit config
openclaw configure

# Restart gateway
openclaw gateway restart

# View logs
tail -f ~/.openclaw/logs/gateway.log
```

---

*Auto-generated by OpenClaw*
